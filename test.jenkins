pipeline {
    agent any
    stages {
        stage('Install Python') {
            // Install Python and set it as the default version
            tools {
                // Name of the Python installation defined in Jenkins
                python 'Python3.9.0'
            }
            steps {
                // Verify that the correct version of Python is installed
                sh 'python --version'
            }
        }
    }
}
