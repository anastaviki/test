pipeline {
    agent any
	tools {
        git 'my-git'
    }
    stages {
	stage('Example') {
            steps {
                withEnv(["PATH+FOO=${tool 'my-git'}/bin"]) {
                    sh 'echo $PATH'
					
                }
            }
        }
	stage('Inst') {
            steps {
                sh 'sudo pip3 install robotframework'
            }
        }
	stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/anastaviki/test.git'
            }
        }

		stage('Install dependencies') {
            steps {
                sh 'pip3 install -r requirements.txt'
            }
        }
		

		 stage('Run Pytest') {
            steps {
                sh 'python3 -m pytest PyTest_HW/PyTest/test_cases.py --html=report.html '
            }
        }
		
		stage('Run Robot Framework Tests') {
            steps {
                sh 'robot -d reports/ RobotFramework_HW/RF_Tests/tests/'
            }
        }
    }
}
