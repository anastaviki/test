pipeline {
    agent any
    tools {
        // Name of the JDK installation defined in Jenkins
        jdk 'JDK8'
        python3 'python3'
    }
    stages {

        stage('Install Python') {
            steps {
            echo 'Preparing'
                // Verify that the correct version of Python is installed
                sh 'python3 --version'
            }
        }
    }
}
